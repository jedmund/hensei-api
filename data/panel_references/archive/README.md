# Archived panel references

These references were captured against party grids that no longer exist in the
database (the golden parties were lost when hensei_dev was rebuilt from a prod
backup on 2026-07-06; 5JPIJg exists but its grid reverted to the pre-testing
state). The captured values are still ground truth — when an equivalent grid
is rebuilt, move the file back up into data/panel_references/ and validate.
